BEGIN TRY

BEGIN TRAN;

-- AlterTable
ALTER TABLE [dbo].[SupplierRule] ADD [deliveryEmail] NVARCHAR(1000) NOT NULL CONSTRAINT [SupplierRule_deliveryEmail_df] DEFAULT '';

COMMIT TRAN;

END TRY
BEGIN CATCH

IF @@TRANCOUNT > 0
BEGIN
    ROLLBACK TRAN;
END;
THROW

END CATCH
