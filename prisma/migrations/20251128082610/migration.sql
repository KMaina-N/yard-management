BEGIN TRY

BEGIN TRAN;

-- AlterTable
ALTER TABLE [dbo].[SupplierRule] ADD [freedCapacity] INT NOT NULL CONSTRAINT [SupplierRule_freedCapacity_df] DEFAULT 0;

COMMIT TRAN;

END TRY
BEGIN CATCH

IF @@TRANCOUNT > 0
BEGIN
    ROLLBACK TRAN;
END;
THROW

END CATCH
