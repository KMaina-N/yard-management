BEGIN TRY

BEGIN TRAN;

-- AlterTable
ALTER TABLE [dbo].[Attachment] ALTER COLUMN [filePath] NVARCHAR(1000) NULL;
ALTER TABLE [dbo].[Attachment] ALTER COLUMN [fileType] NVARCHAR(1000) NULL;

COMMIT TRAN;

END TRY
BEGIN CATCH

IF @@TRANCOUNT > 0
BEGIN
    ROLLBACK TRAN;
END;
THROW

END CATCH
